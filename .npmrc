# typescript@7 conflicts with @solana/* peerOptional typescript@^5; without this `npm ci` fails with ERESOLVE.
legacy-peer-deps=true
# Third-party lifecycle scripts must never run during a normal install.
# Use scripts/sandbox-install.sh with an explicit package allowlist if required.
ignore-scripts=true
